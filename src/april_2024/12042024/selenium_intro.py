# call the selenium
# use the selenium commands
# selenium code -(python, java) -> api http request -> chromeDriver
# create a session
# send the commands - navigate to a url
# * if you're using selenium < 4 : we use to set the Driver path
# * if you' using selenium > 4: Driver path is not needed they will handle automatically'